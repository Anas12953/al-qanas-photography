(()=>{
  const html=document.documentElement,body=document.body,qs=(s,e=document)=>e.querySelector(s),qsa=(s,e=document)=>[...e.querySelectorAll(s)],clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
  const reduced=matchMedia('(prefers-reduced-motion:reduce)').matches,fine=matchMedia('(pointer:fine)').matches;
  let loaderDone=false;
  let theme=localStorage.getItem('alqanas-theme')||(matchMedia('(prefers-color-scheme: light)').matches?'light':'dark');
  let lang=localStorage.getItem('alqanas-lang')||'en';
  function applyTheme(v){theme=v;html.dataset.theme=v;localStorage.setItem('alqanas-theme',v);const m=qs('meta[name="theme-color"]');if(m)m.content=v==='dark'?'#0A0908':'#EAE0D5'}
  function applyLang(v){lang=v;html.lang=v;html.dir=v==='ar'?'rtl':'ltr';qsa('[data-en][data-ar]').forEach(el=>{if(!el.hasAttribute('data-typewriter'))el.textContent=el.dataset[v]});const b=qs('#langBtn');if(b)b.textContent=v==='ar'?'EN':'AR';localStorage.setItem('alqanas-lang',v);restartTypewriter()}
  applyTheme(theme);applyLang(lang);
  async function hydrateFullYasser(){
    const imgs=qsa('img[src="assets/yasser-ismail.jpg"]');
    if(!imgs.length)return;
    try{
      const names=Array.from({length:7},(_,i)=>`assets/yasser-full/part-${String(i+1).padStart(2,'0')}.txt`);
      const parts=await Promise.all(names.map(n=>fetch(n,{cache:'force-cache'}).then(r=>{if(!r.ok)throw new Error(n);return r.text()})));
      const bin=atob(parts.join(''));
      const bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);
      const url=URL.createObjectURL(new Blob([bytes],{type:'image/avif'}));
      imgs.forEach(img=>{img.src=url;img.dataset.fullres='1'});
    }catch(err){console.warn('Full portrait fallback remains active.',err)}
  }
  hydrateFullYasser();
  const yr=qs('#year');if(yr)yr.textContent=new Date().getFullYear();
  const loader=qs('#loader'),loaderWord=qs('#loaderWord'),loaderBar=qs('#loaderBar'),loaderPct=qs('#loaderPct');
  function finishLoader(){loaderDone=true;loader?.classList.add('done');body.classList.remove('locked');setTimeout(()=>restartTypewriter(),120)}
  if(loader){body.classList.add('locked');const words=lang==='ar'?['تركيز','لحظة','حكاية','ذاكرة','القنّاص']:['FOCUS','MOMENT','STORY','MEMORY','AL QANAS'];let idx=0,start=performance.now(),dur=reduced?550:3600,last=-1;function frame(now){const p=clamp((now-start)/dur,0,1),e=1-Math.pow(1-p,3);if(loaderBar)loaderBar.style.transform=`scaleX(${e})`;if(loaderPct)loaderPct.textContent=String(Math.round(e*100)).padStart(2,'0');const next=Math.min(words.length-1,Math.floor(p*words.length));if(next!==idx&&next!==last){idx=next;last=next;loaderWord?.classList.add('swap');setTimeout(()=>{if(loaderWord)loaderWord.textContent=words[idx]},130);setTimeout(()=>loaderWord?.classList.remove('swap'),330)}if(p<1)requestAnimationFrame(frame);else setTimeout(finishLoader,reduced?0:120)}if(loaderWord)loaderWord.textContent=words[0];requestAnimationFrame(frame)}else loaderDone=true;
  let typeTimer=[];function clearType(){typeTimer.forEach(clearTimeout);typeTimer=[]}
  function typeOne(el,text,startDelay=0,speed=54){el.textContent='';el.setAttribute('aria-label',text);let i=0;const caret=document.createElement('i');caret.className='type-caret';function tick(){el.textContent=text.slice(0,i);el.appendChild(caret);i++;if(i<=text.length)typeTimer.push(setTimeout(tick,speed));else typeTimer.push(setTimeout(()=>caret.remove(),700))}typeTimer.push(setTimeout(tick,startDelay))}
  function restartTypewriter(){const els=qsa('[data-typewriter]');if(!els.length||!loaderDone)return;clearType();els.forEach((el,i)=>typeOne(el,el.dataset[lang]||'',i*520,lang==='ar'?70:52))}
  let ctx=null,soundOn=localStorage.getItem('alqanas-sound')!=='off',hoverStamp=0;const soundBtn=qs('#soundBtn');
  function audio(){if(!soundOn)return null;if(!ctx){const AC=window.AudioContext||window.webkitAudioContext;if(!AC)return null;ctx=new AC()}if(ctx.state==='suspended')ctx.resume().catch(()=>{});return ctx}
  function tone(f=420,d=.05,v=.028,type='sine',end=null){const c=audio();if(!c)return;const t=c.currentTime,o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(f,t);if(end)o.frequency.exponentialRampToValueAtTime(end,t+d);g.gain.setValueAtTime(.0001,t);g.gain.exponentialRampToValueAtTime(v,t+.008);g.gain.exponentialRampToValueAtTime(.0001,t+d);o.connect(g);g.connect(c.destination);o.start(t);o.stop(t+d+.02)}
  const clickS=()=>{tone(255,.06,.032,'triangle',410);setTimeout(()=>tone(650,.04,.016,'sine',760),28)},hoverS=()=>{if(!ctx)return;const n=performance.now();if(n-hoverStamp<75)return;hoverStamp=n;tone(520,.026,.008,'sine',610)};
  addEventListener('pointerdown',()=>audio(),{once:true,capture:true});qsa('a,button').forEach(el=>{el.addEventListener('mouseenter',hoverS);el.addEventListener('click',()=>{if(!el.matches('#soundBtn'))clickS()})});
  if(soundBtn){soundBtn.classList.toggle('muted-sound',!soundOn);soundBtn.addEventListener('click',()=>{if(soundOn){clickS();setTimeout(()=>{soundOn=false;localStorage.setItem('alqanas-sound','off');soundBtn.classList.add('muted-sound')},70)}else{soundOn=true;localStorage.setItem('alqanas-sound','on');soundBtn.classList.remove('muted-sound');audio();setTimeout(()=>{tone(340,.09,.028,'sine',540);tone(680,.1,.018,'sine',900)},35)}})}
  qs('#themeBtn')?.addEventListener('click',()=>{applyTheme(theme==='dark'?'light':'dark');tone(330,.08,.022,'sine',650)});qs('#langBtn')?.addEventListener('click',()=>{lang=lang==='en'?'ar':'en';applyLang(lang);tone(430,.055,.02,'sine',570)});const navMenu=qs('#navMenu');qs('#navHamb')?.addEventListener('click',()=>navMenu?.classList.toggle('open'));
  if(fine&&!reduced){const ring=qs('.cursor-ring'),dot=qs('.cursor-dot');if(ring&&dot){let mx=-100,my=-100,rx=-100,ry=-100;addEventListener('mousemove',e=>{mx=e.clientX;my=e.clientY;body.classList.add('cursor-ready')},{passive:true});function cf(){rx+=(mx-rx)*.18;ry+=(my-ry)*.18;ring.style.transform=`translate3d(${rx}px,${ry}px,0) translate(-50%,-50%)`;dot.style.transform=`translate3d(${mx}px,${my}px,0) translate(-50%,-50%)`;requestAnimationFrame(cf)}cf();qsa('a,button,.tilt,.magnetic').forEach(el=>{el.addEventListener('mouseenter',()=>ring.classList.add('hot'));el.addEventListener('mouseleave',()=>ring.classList.remove('hot'))})}}
  const canvas=qs('#flickerGrid'),g=canvas?.getContext('2d',{alpha:true});let W=0,H=0,dpr=1,cols=0,rows=0,cells=null,last=0;const size=3,gap=10,step=size+gap;
  function resizeGrid(){if(!g)return;dpr=Math.min(devicePixelRatio||1,1.5);W=innerWidth;H=innerHeight;canvas.width=W*dpr;canvas.height=H*dpr;canvas.style.width=W+'px';canvas.style.height=H+'px';g.setTransform(dpr,0,0,dpr,0,0);cols=Math.ceil(W/step)+1;rows=Math.ceil(H/step)+1;cells=new Float32Array(cols*rows);for(let i=0;i<cells.length;i++)cells[i]=Math.random()*.07}
  function drawGrid(now){if(!g)return;if(now-last>90){last=now;g.clearRect(0,0,W,H);const c=theme==='dark'?[145,171,199]:[49,93,140];for(let y=0;y<rows;y++)for(let x=0;x<cols;x++){let i=y*cols+x;if(Math.random()<.05)cells[i]=.025+Math.random()*.16;else cells[i]*=.92;if(cells[i]<.009)continue;g.fillStyle=`rgba(${c[0]},${c[1]},${c[2]},${cells[i]})`;g.fillRect(x*step,y*step,size,size)}}if(!reduced)requestAnimationFrame(drawGrid)}resizeGrid();requestAnimationFrame(drawGrid);addEventListener('resize',resizeGrid,{passive:true});addEventListener('pointermove',e=>{body.style.setProperty('--mx',`${e.clientX/innerWidth*100}%`);body.style.setProperty('--my',`${e.clientY/innerHeight*100}%`)},{passive:true});
  const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -4% 0px'});qsa('.reveal,.reveal-left,.image-reveal').forEach(el=>io.observe(el));
  if(fine&&!reduced){qsa('.tilt').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(1200px) rotateX(${-py*4.8}deg) rotateY(${px*5.8}deg) translateY(-3px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});qsa('.glare').forEach(el=>el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.setProperty('--gx',`${e.clientX-r.left}px`);el.style.setProperty('--gy',`${e.clientY-r.top}px`)}));qsa('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect(),x=e.clientX-(r.left+r.width/2),y=e.clientY-(r.top+r.height/2);el.style.transform=`translate(${x*.12}px,${y*.12}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});qsa('.depth-orb').forEach(orb=>addEventListener('mousemove',e=>{const r=orb.parentElement.getBoundingClientRect(),x=(e.clientX-r.left)/Math.max(r.width,1)-.5,y=(e.clientY-r.top)/Math.max(r.height,1)-.5;orb.style.transform=`translate3d(${x*18}px,${y*14}px,0) rotateX(${-y*15}deg) rotateY(${x*18}deg)`},{passive:true}))}
  const ticker=qs('.ticker-row'),tg=qs('.ticker-group');let tickX=0,tickLast=performance.now();function tickerFrame(now){if(ticker&&tg){const dt=Math.min(40,now-tickLast);tickLast=now;const dir=html.dir==='rtl'?1:-1;tickX+=dir*dt*.045;const w=tg.getBoundingClientRect().width;if(w>0){if(dir<0&&tickX<=-w)tickX+=w;if(dir>0&&tickX>=0)tickX-=w;ticker.style.transform=`translate3d(${tickX}px,0,0)`}}requestAnimationFrame(tickerFrame)}requestAnimationFrame(tickerFrame);
  const cards=qsa('.depth-card');let active=0,autoTimer=null,dragStart=null;
  function renderDepth(){if(!cards.length)return;const n=cards.length;cards.forEach((c,i)=>{let d=i-active;if(d>n/2)d-=n;if(d<-n/2)d+=n;c.dataset.pos=String(clamp(d,-2,2))})}
  function setDepth(n){if(!cards.length)return;active=(n+cards.length)%cards.length;renderDepth();tone(380,.045,.015,'sine',520);resetDepthAuto()}
  function resetDepthAuto(){clearInterval(autoTimer);if(cards.length&&!reduced)autoTimer=setInterval(()=>setDepth(active+1),4300)}
  if(cards.length){renderDepth();resetDepthAuto();qs('#depthPrev')?.addEventListener('click',()=>setDepth(active-1));qs('#depthNext')?.addEventListener('click',()=>setDepth(active+1));const sc=qs('.depth-carousel');sc?.addEventListener('pointerdown',e=>{dragStart=e.clientX;sc.setPointerCapture?.(e.pointerId)});sc?.addEventListener('pointerup',e=>{if(dragStart==null)return;const dx=e.clientX-dragStart;if(Math.abs(dx)>45)setDepth(active+(dx<0?1:-1));dragStart=null})}
  const stackCards=qsa('.service-stack-card');function updateStack(){if(!stackCards.length||innerWidth<780)return;stackCards.forEach((c,i)=>{const r=c.getBoundingClientRect(),top=105+i*20,delta=clamp((top-r.top)/240,0,1);c.style.transform=`scale(${1-delta*.028}) translateY(${delta*i*4}px)`;c.style.filter=`brightness(${1-delta*.06})`})}addEventListener('scroll',()=>requestAnimationFrame(updateStack),{passive:true});addEventListener('resize',updateStack);updateStack();
  const wipe=qs('#pageWipe');qsa('a[data-page-link]').forEach(a=>a.addEventListener('click',e=>{if(e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;e.preventDefault();const href=a.href;wipe?.style.setProperty('--wipe-x',`${e.clientX||innerWidth/2}px`);wipe?.style.setProperty('--wipe-y',`${e.clientY||innerHeight/2}px`);tone(150,.16,.022,'sine',520);wipe?.classList.add('go');setTimeout(()=>location.href=href,reduced?0:620)}));
  const galleryItems=qsa('.gitem'),filterBtns=qsa('.filter-btn');filterBtns.forEach(b=>b.addEventListener('click',()=>{filterBtns.forEach(x=>x.classList.remove('active'));b.classList.add('active');const f=b.dataset.filter;galleryItems.forEach(item=>item.classList.toggle('hide',f!=='all'&&item.dataset.cat!==f));tone(430,.05,.018,'sine',560)}));const lightbox=qs('#lightbox'),lbImg=qs('#lbImg'),lbTitle=qs('#lbTitle'),lbCount=qs('#lbCount');let li=0;function show(i){if(!galleryItems.length)return;li=(i+galleryItems.length)%galleryItems.length;const item=galleryItems[li],im=qs('img',item);if(lbImg)lbImg.src=im.src;if(lbTitle)lbTitle.textContent=qs('b',item)?.textContent||'Gallery';if(lbCount)lbCount.textContent=`${String(li+1).padStart(2,'0')} / ${String(galleryItems.length).padStart(2,'0')}`}function openLb(i){if(!lightbox)return;show(i);lightbox.classList.add('open');body.classList.add('locked');tone(280,.07,.025,'triangle',480)}function closeLb(){if(!lightbox?.classList.contains('open'))return;lightbox.classList.remove('open');body.classList.remove('locked');tone(500,.06,.018,'sine',310)}galleryItems.forEach((it,i)=>it.addEventListener('click',()=>openLb(i)));qs('#lbClose')?.addEventListener('click',closeLb);qs('#lbPrev')?.addEventListener('click',()=>show(li-1));qs('#lbNext')?.addEventListener('click',()=>show(li+1));qs('.lightbox-bg')?.addEventListener('click',closeLb);addEventListener('keydown',e=>{if(e.key==='Escape')closeLb();if(lightbox?.classList.contains('open')){if(e.key==='ArrowLeft')show(li-1);if(e.key==='ArrowRight')show(li+1)}});
})();